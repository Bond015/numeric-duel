# 📦 Инструкция по деплою игры

## Разделение: Фронтенд + Сервер

Игра состоит из двух частей:
1. **Фронтенд**: HTML, JS, CSS (выдаётся статикой)
2. **Сервер**: Node.js с Socket.io (для мультиплеера)

Для публикации их нужно разнести:
- Фронтенд → Netlify / Vercel / GitHub Pages
- Сервер → Railway / Render

---

## 🎯 Вариант 1: Быстрый деплой (для тестирования)

### Фронтенд: Netlify

1. Зайди на https://netlify.com
2. Зарегистрируйся (можно через GitHub)
3. Нажми "Add new site" → "Deploy manually"
4. Перетащи всю папку проекта (кроме `server/` и `node_modules/`)
5. Готово! Получишь URL типа `https://your-game.netlify.app`

### Сервер: Railway

1. Зайди на https://railway.app
2. Зарегистрируйся (можно через GitHub)
3. "New Project" → "Deploy from GitHub repo"
4. Выбери папку `server/` как корень проекта
5. Railway сам установит зависимости
6. Получишь URL типа `https://your-server.railway.app`

**Важно**: После деплоя сервера нужно обновить URL в `game.js`:
```javascript
// Замени localhost на URL твоего сервера
socket = io('https://your-server.railway.app');
```

---

## 🎯 Вариант 2: GitHub Pages (бесплатно)

### Фронтенд: GitHub Pages

1. Создай репозиторий на GitHub
2. Залей файлы (кроме `server/`)
3. Settings → Pages → "Deploy from a branch" → main
4. Готово! `https://твой-username.github.io/твой-repo`

### Сервер: Render

1. Зайди на https://render.com
2. "New" → "Web Service"
3. Подключи GitHub репозиторий
4. Settings:
   - **Root Directory**: `server`
   - **Build Command**: `npm install`
   - **Start Command**: `node app.js`
5. Получишь URL

---

## 🎯 Вариант 3: Vercel (для фронтенда)

### Фронтенд: Vercel

1. Установи Vercel CLI:
```bash
npm i -g vercel
```

2. В корне проекта (где `index.html`):
```bash
vercel
```

3. Следуй инструкциям
4. Готово! `https://your-game.vercel.app`

---

## ⚙️ Настройка переменных окружения

Для сервера нужно установить переменные (если нужно):

### Railway / Render
- `PORT` - обычно устанавливается автоматически
- Можно добавить свои переменные в настройках проекта

---

## 🔗 Объединение фронтенда и сервера

После деплоя обоих частей, нужно обновить `game.js`:

Найди где создаётся socket:
```javascript
socket = io();
```

Замени на:
```javascript
socket = io('https://your-server-url.com');
```

---

## 🧪 Тестирование после деплоя

1. Открой фронтенд URL
2. Попробуй одиночную игру
3. Попробуй мультиплеер (нужно 2 вкладки)
4. Проверь что сервер подключён

---

## 💰 Стоимость

### Бесплатно:
- Netlify (фронтенд) - 100GB трафика
- GitHub Pages - бесплатно
- Vercel - бесплатно для личных проектов
- Railway - $5 кредитов в месяц бесплатно
- Render - бесплатно с ограничениями

### Если трафик вырастет:
- Railway: ~$5-10/месяц
- Render: ~$7/месяц за базовый тариф

---

## 📝 Чеклист деплоя

- [ ] Фронтенд задеплоен
- [ ] Сервер задеплоен
- [ ] URL сервера обновлён в `game.js`
- [ ] Одиночная игра работает
- [ ] Мультиплеер работает
- [ ] HTTPS включен (обычно автоматически)
- [ ] CORS настроен (уже настроен в `server/app.js`)

---

## 🆘 Проблемы и решения

### Сокет не подключается
- Проверь URL сервера в `game.js`
- Проверь что сервер запущен
- Проверь CORS настройки

### Игра не грузится
- Проверь что все файлы загружены
- Открой консоль браузера (F12)
- Проверь ошибки

### Мультиплеер не работает
- Нужно минимум 2 игрока
- Проверь что оба подключаются к одному серверу
- Проверь логи сервера

---

## 🚀 Следующий шаг

После успешного деплоя переходи к публикации на игровых площадках!

