# 🎮 Локальный мультиплеер через Яндекс SDK

## ❓ Можно ли использовать локальный мультиплеер Яндекс Игр?

### 🔍 Что такое "локальный мультиплеер" в Яндекс Играх:

**Локальный мультиплеер** в Яндекс Играх — это когда:
- ✅ Два игрока играют **на одном устройстве**
- ✅ Например: два игрока на одном телефоне/планшете
- ✅ Или: два игрока на одной странице (split-screen)

### ❌ Ограничения локального мультиплеера:

1. **Не подходит для онлайн-игры:**
   - Локальный мультиплеер **НЕ** позволяет играть игрокам, находящимся на разных устройствах
   - Игроки должны быть **физически рядом** (один экран, один браузер)

2. **Ваш случай:**
   - Ваша игра требует **онлайн мультиплеера** (игроки на разных устройствах)
   - Нужен **сервер** для поиска соперников и синхронизации
   - **Локальный мультиплеер не подходит** для вашей архитектуры

---

## 🎯 Ваш случай: Онлайн мультиплеер

### ✅ Текущая архитектура (правильная):

```
Игрок 1 (Браузер 1)  ←→  Railway Сервер  ←→  Игрок 2 (Браузер 2)
```

**Преимущества:**
- ✅ Игроки могут быть в разных местах
- ✅ Поиск случайных соперников
- ✅ Глобальный лидерборд
- ✅ Сохранение статистики

### ❌ Локальный мультиплеер (не подходит):

```
Игрок 1 + Игрок 2  →  Один браузер/устройство
```

**Ограничения:**
- ❌ Игроки должны быть рядом (один экран)
- ❌ Нет поиска случайных соперников
- ❌ Нет глобального лидерборда

---

## 🔧 Альтернатива: Яндекс SDK + Ваш сервер

### Гибридный подход:

1. **Использовать Яндекс SDK для:**
   - ✅ Получения информации об игроке (никнейм, ID)
   - ✅ Лидерборда (если есть API)
   - ✅ Авторизации

2. **Использовать ваш сервер для:**
   - ✅ Поиска соперников (matchmaking)
   - ✅ Синхронизации игрового состояния
   - ✅ Хранения статистики

### Пример интеграции:

```javascript
// Используем Яндекс SDK для получения данных игрока
const player = await yaGamesSDK.getPlayer();
const playerName = player.getName(); // Никнейм из Яндекс ID

// Используем ваш сервер для мультиплеера
socket.emit('find-match', {
    playerId: player.getUniqueID(), // ID из Яндекс
    name: playerName, // Никнейм из Яндекс
    // ... остальные данные
});
```

---

## 📋 Итоговые рекомендации:

### ✅ Оставить текущую архитектуру (рекомендуется):

1. **Добавить Railway сервер в CSP** (как описано в `YANDEX_MULTIPLAYER_CSP.md`)
2. **Использовать Яндекс SDK для:**
   - Автоматического получения никнейма игрока
   - Авторизации
   - Других функций SDK (если нужны)

3. **Использовать ваш сервер для:**
   - Поиска соперников
   - Синхронизации игры
   - Лидерборда

### ❌ НЕ переходить на локальный мультиплеер:

- Локальный мультиплеер не подходит для вашей игры
- Ваша архитектура уже правильная для онлайн-игры
- Нужно только добавить сервер в CSP исключения

---

## 🔗 Полезные ссылки

- **Документация Яндекс SDK:** https://yandex.ru/dev/games/doc/ru/sdk/sdk-about
- **Инструкция по CSP:** `YANDEX_MULTIPLAYER_CSP.md`

---

## ✅ Вывод

**Локальный мультиплеер Яндекс Игр НЕ подходит** для вашей игры, так как:
1. Он работает только для игроков на одном устройстве
2. Ваша игра требует онлайн-мультиплеера (разные устройства)
3. Текущая архитектура с Railway сервером правильная

**Что нужно сделать:**
- ✅ Добавить Railway сервер в CSP исключения
- ✅ Использовать Яндекс SDK для авторизации и данных игрока
- ✅ Оставить ваш сервер для мультиплеера

---

**Ваша текущая архитектура оптимальна! 🎮**


