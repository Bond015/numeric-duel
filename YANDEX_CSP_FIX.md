# 🔒 Исправление CSP проблемы для Яндекс Игр

## ❌ Проблема

Яндекс Игры блокируют загрузку Socket.io с внешнего CDN (`cdn.socket.io`) из-за политики безопасности (CSP - Content Security Policy).

**Ошибка:**
```
Refused to load the script 'https://cdn.socket.io/4.5.4/socket.io.min.js' 
because it violates the following Content Security Policy directive: 
"script-src 'self'..."
```

## ✅ Решение

Использовать **локальную копию Socket.io** в ZIP архиве игры, чтобы скрипт загружался с того же домена (`'self'`).

---

## 📝 Что было сделано

### 1. Скачан Socket.io локально
- Файл: `socket.io.min.js` (версия 4.5.4)
- Размер: ~43 KB
- Расположение: корень проекта

### 2. Обновлен `index.html`
**Было:**
```html
<script src="https://cdn.socket.io/4.5.4/socket.io.min.js"></script>
```

**Стало:**
```html
<!-- Socket.io: локальная копия для Яндекс Игр (CSP совместимость) -->
<script src="socket.io.min.js"></script>
```

### 3. Обновлен скрипт создания ZIP
Добавлен `socket.io.min.js` в список файлов для архива.

### 4. Создан новый ZIP архив
- Файл: `numeric-duel-game.zip`
- Размер: ~40 KB (было ~27 KB)
- Включает: Socket.io локально

---

## 🚀 Что нужно сделать сейчас

### Вариант 1: Использовать ZIP архив (рекомендуется для Яндекс Игр)

1. **Загрузите новый ZIP архив:**
   - Файл: `numeric-duel-game.zip`
   - Замените старый архив в форме Яндекс Игр

2. **Проверьте:**
   - Игра загружается без ошибок CSP
   - Socket.io работает (мультиплеер подключается)

### Вариант 2: Использовать URL (если доступно)

Если в Яндекс Играх есть возможность указать URL вместо ZIP:
- URL: `https://numeric-duel.vercel.app`
- После пуша в Git, игра автоматически обновится на Vercel с локальным Socket.io

---

## ⚠️ Важно

1. **Файл `socket.io.min.js` в `.gitignore`:**
   - Файл не коммитится в репозиторий (слишком большой)
   - Нужно скачивать локально при создании ZIP

2. **Для обновления Socket.io:**
   ```powershell
   Invoke-WebRequest -Uri "https://cdn.socket.io/4.5.4/socket.io.min.js" -OutFile "socket.io.min.js"
   ```

3. **Версия Socket.io:**
   - Текущая версия: 4.5.4
   - Должна совпадать с версией на сервере

---

## 📋 Проверка после исправления

После загрузки нового ZIP в Яндекс Игры:

- ✅ Нет ошибок CSP в консоли
- ✅ Socket.io загружается без ошибок
- ✅ Мультиплеер подключается к серверу
- ✅ Все функции работают

---

## 🔄 Если нужно обновить Socket.io

Если в будущем понадобится обновить версию Socket.io:

1. **Скачать новую версию:**
   ```powershell
   Invoke-WebRequest -Uri "https://cdn.socket.io/НОВАЯ_ВЕРСИЯ/socket.io.min.js" -OutFile "socket.io.min.js"
   ```

2. **Пересоздать ZIP:**
   ```powershell
   .\create-game-zip.ps1
   ```
   
   Или вручную:
   ```powershell
   # Удалить старый архив
   Remove-Item numeric-duel-game.zip -Force
   
   # Создать новый (команда из create-game-zip.ps1)
   ```

3. **Загрузить новый ZIP в Яндекс Игры**

---

## ✅ Результат

- ✅ CSP проблема решена
- ✅ Socket.io работает локально
- ✅ Мультиплеер функционирует
- ✅ Игра готова для Яндекс Игр

---

**Проблема решена! 🎉**
