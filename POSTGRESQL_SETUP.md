# 📊 Настройка PostgreSQL для постоянного хранения рейтинга

## ✅ Что было сделано

1. **Создан модуль `server/db.js`** - для работы с PostgreSQL
2. **Установлен пакет `pg`** - PostgreSQL клиент для Node.js
3. **Обновлен `server/app.js`** - теперь использует PostgreSQL вместо файла
4. **Все функции обновлены** - работают с async/await для работы с БД

---

## 🔧 Настройка на Railway

### Шаг 1: Проверьте PostgreSQL сервис

На скриншоте видно, что у вас уже есть сервис **Postgres** в Railway. Убедитесь, что он активен и работает.

### Шаг 2: Переменные окружения

Railway **автоматически создает** переменные окружения для PostgreSQL:
- `DATABASE_URL` - полная строка подключения
- `PGHOST`, `PGPORT`, `PGUSER`, `PGPASSWORD`, `PGDATABASE`

**Проверка:**
1. Зайдите в Railway Dashboard
2. Выберите ваш проект
3. Перейдите в "Variables" или настройки сервиса Postgres
4. Убедитесь, что `DATABASE_URL` существует

### Шаг 3: Обновите код на сервере

Код уже обновлен! Просто нужно задеплоить:

```bash
git add .
git commit -m "Переход на PostgreSQL для хранения рейтинга"
git push
```

Railway автоматически обновит сервер.

---

## 🧪 Тестирование

### Тест 1: Проверка подключения к БД

После деплоя проверьте логи Railway:
```
✅ Database initialized successfully
✅ Loaded X players from PostgreSQL
📊 PostgreSQL подключен, лидерборд загружен
```

Если видите ошибки подключения, проверьте переменные окружения.

### Тест 2: Проверка сохранения данных

1. Сыграйте несколько матчей в мультиплеере
2. Проверьте логи - должны быть сообщения:
   ```
   💾 Saved player [nickname] ([playerId]) to database
   ```

### Тест 3: Проверка после перезапуска

1. Сделайте `git push` (это перезапустит сервер)
2. Проверьте, что данные сохранились после перезапуска
3. Проверьте лидерборд - данные должны остаться

---

## 🔍 Проверка базы данных

### Через Railway Dashboard

1. Зайдите в Railway Dashboard
2. Выберите сервис **Postgres**
3. Перейдите в "Data" или "Database"
4. Вы должны увидеть таблицу `leaderboard` с данными

### Через SQL запрос

В Railway можно выполнить SQL запросы:
```sql
-- Посмотреть все игроки
SELECT * FROM leaderboard ORDER BY rating DESC LIMIT 10;

-- Проверить количество игроков
SELECT COUNT(*) FROM leaderboard;

-- Проверить структуру таблицы
SELECT column_name, data_type FROM information_schema.columns 
WHERE table_name = 'leaderboard';
```

---

## 📋 Структура таблицы

Таблица `leaderboard` имеет следующую структуру:

```sql
CREATE TABLE leaderboard (
    player_id VARCHAR(255) PRIMARY KEY,
    nickname VARCHAR(255) NOT NULL,
    wins INTEGER DEFAULT 0,
    losses INTEGER DEFAULT 0,
    rating INTEGER DEFAULT 0,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**Индексы:**
- `idx_rating` - для быстрой сортировки по рейтингу
- `idx_updated_at` - для сортировки по времени обновления

---

## ⚠️ Важные замечания

1. **Данные теперь сохраняются навсегда** - при перезапуске сервера они не будут потеряны
2. **Кеш в памяти** - сервер использует кеш для быстрого доступа, но данные синхронизируются с БД
3. **Автоматическое создание таблицы** - при первом запуске таблица создается автоматически
4. **Обратная совместимость** - старые данные из `leaderboard.json` не будут перенесены автоматически (если они есть)

---

## 🔄 Миграция данных (опционально)

Если у вас есть данные в старом файле `leaderboard.json`, можно написать скрипт для их переноса в PostgreSQL. Но обычно это не требуется, так как данные уже должны быть в БД, если вы играли на сервере.

---

## 🐛 Устранение проблем

### Ошибка подключения к БД

**Проблема:** `Error connecting to database`

**Решение:**
1. Проверьте, что сервис Postgres активен в Railway
2. Проверьте переменные окружения `DATABASE_URL`
3. Убедитесь, что сервис numeric-duel связан с Postgres (в Railway они должны быть в одном проекте)

### Таблица не создается

**Проблема:** `relation "leaderboard" does not exist`

**Решение:**
1. Проверьте логи - должна быть строка `✅ Database initialized successfully`
2. Если ошибка, проверьте права доступа к БД
3. Попробуйте перезапустить сервер

### Данные не сохраняются

**Проблема:** Данные теряются после перезапуска

**Решение:**
1. Проверьте логи - должны быть сообщения `💾 Saved player...`
2. Проверьте подключение к БД
3. Проверьте, что `updateGlobalLeaderboard()` вызывается с await

---

## ✅ Готово!

После настройки PostgreSQL, ваш рейтинг будет сохраняться **навсегда** и не будет теряться при перезапуске сервера.

**Следующие шаги:**
1. Задеплойте изменения: `git add . && git commit -m "PostgreSQL setup" && git push`
2. Проверьте логи Railway
3. Протестируйте сохранение данных
4. Наслаждайтесь постоянным хранением рейтинга! 🎉
