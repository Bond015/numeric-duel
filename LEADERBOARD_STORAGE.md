# 📊 Анализ сохранения данных лидерборда

## ✅ Текущая реализация

### Как работает сохранение:
1. **Файл хранилища**: `server/leaderboard.json`
2. **Сохранение**: Данные сохраняются в файл **после каждого обновления рейтинга** (в функции `updateGlobalLeaderboard()`)
3. **Загрузка**: Данные загружаются из файла **при старте сервера** (в функции `loadLeaderboard()`)

### Код сохранения:
```javascript
// Сохранение после каждого обновления
function updateGlobalLeaderboard(playerId, nickname, won) {
    // ... обновление данных ...
    saveLeaderboard(); // ✅ Вызывается после каждого обновления
}

// Загрузка при старте
loadLeaderboard(); // ✅ Вызывается при запуске сервера
```

---

## ⚠️ Потенциальная проблема

### Эфемерная файловая система на Railway

**Проблема**: Railway (и большинство PaaS платформ) используют **эфемерные файловые системы** для контейнеров.

Это означает:
- ❌ При **перезапуске контейнера** (например, после `git push`) файл `leaderboard.json` может быть **удален**
- ❌ При **обновлении сервера** (через Railway dashboard) данные могут быть **потеряны**
- ❌ При **автоматическом перезапуске** Railway (например, после периода неактивности) данные могут быть **утеряны**

### Когда это происходит:
- 🔄 После каждого `git push` (автоматический деплой)
- 🔄 При перезапуске сервера через Railway dashboard
- 🔄 При автоматическом перезапуске Railway (иногда происходит автоматически)
- 🔄 При обновлении зависимостей или изменении конфигурации

---

## 🔍 Проверка текущей ситуации

### Тест 1: Проверьте, сохраняются ли данные
1. Зайдите на сервер Railway
2. Проверьте логи: должны быть сообщения `✅ Loaded X players from leaderboard` при старте
3. Сыграйте несколько матчей
4. Проверьте логи: должны быть сообщения `💾 Saved X players to leaderboard` после каждого матча

### Тест 2: Проверьте после перезапуска
1. Сделайте `git push` (это перезапустит сервер)
2. Проверьте, сохранились ли данные после перезапуска

---

## ✅ Решения

### Вариант 1: Railway Persistent Volume (рекомендуется для простоты)

Railway поддерживает постоянное хранилище через Volumes:

1. **В Railway Dashboard:**
   - Зайдите в ваш проект
   - Перейдите в "Settings" → "Volumes"
   - Создайте новый Volume
   - Подключите его к серверу

2. **Измените путь к файлу в коде:**
   ```javascript
   // Если Railway Volume монтируется в /data
   const LEADERBOARD_FILE = process.env.LEADERBOARD_PATH || path.join(__dirname, 'leaderboard.json');
   ```

3. **Добавьте переменную окружения:**
   - В Railway Dashboard: Settings → Variables
   - Добавьте: `LEADERBOARD_PATH=/data/leaderboard.json`

**Плюсы**: Просто, данные сохраняются навсегда
**Минусы**: Railway Volumes могут быть платными на некоторых планах

---

### Вариант 2: База данных (PostgreSQL, MongoDB)

#### A. Railway PostgreSQL (рекомендуется)

Railway предлагает PostgreSQL как сервис:

1. **Добавьте PostgreSQL в Railway:**
   - В Railway Dashboard: "New" → "Database" → "Add PostgreSQL"

2. **Установите пакет:**
   ```bash
   npm install pg
   ```

3. **Измените код для использования БД** (см. пример ниже)

#### B. MongoDB Atlas (бесплатно)

1. Создайте аккаунт на [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)
2. Создайте бесплатный кластер
3. Получите connection string
4. Установите пакет: `npm install mongodb`
5. Измените код для использования MongoDB

---

### Вариант 3: Внешний сервис (Redis, Supabase)

Можно использовать внешние сервисы для хранения данных.

---

## 🔧 Быстрое решение: Использование Railway PostgreSQL

### Шаг 1: Добавить PostgreSQL в Railway
1. Зайдите в Railway Dashboard
2. В вашем проекте: "New" → "Database" → "Add PostgreSQL"
3. Railway автоматически создаст переменные окружения:
   - `DATABASE_URL`
   - `PGHOST`
   - `PGPORT`
   - `PGUSER`
   - `PGPASSWORD`
   - `PGDATABASE`

### Шаг 2: Установить пакет
```bash
cd server
npm install pg
```

### Шаг 3: Изменить код server/app.js

Заменить файловое хранилище на PostgreSQL (нужно будет создать новый файл с функциями для работы с БД).

---

## 📝 Рекомендация

**Для начала**: Проверьте, действительно ли данные теряются после перезапуска:
1. Сыграйте несколько матчей
2. Сделайте `git push`
3. Проверьте, сохранились ли данные после перезапуска

**Если данные теряются**: Используйте **Railway PostgreSQL** или **Railway Volumes** для постоянного хранения.

**Если данные сохраняются**: Отлично! Продолжайте использовать текущую реализацию, но помните о потенциальных рисках.

---

## 🔗 Полезные ссылки

- [Railway Volumes Documentation](https://docs.railway.app/develop/storage)
- [Railway PostgreSQL](https://docs.railway.app/databases/postgresql)
- [Railway Environment Variables](https://docs.railway.app/develop/variables)

---

## ⚠️ Важно

**Текущая реализация работает**, но **не гарантирует постоянное хранение** на Railway из-за эфемерной файловой системы. Если данные важны, рекомендуется перейти на постоянное хранилище (Volumes или БД).
