# 🔧 Настройка PostgreSQL в Railway - Решение проблемы подключения

## ⚠️ Проблема

Если вы видите ошибку `ECONNREFUSED 127.0.0.1:5432`, это означает, что сервис `numeric-duel` не может найти переменные окружения для подключения к PostgreSQL.

---

## ✅ Решение: Настройка переменных окружения в Railway

### Способ 1: Автоматическое связывание (рекомендуется)

Railway может автоматически передавать переменные окружения между сервисами, если они правильно связаны:

1. **Зайдите в Railway Dashboard**
2. **Выберите ваш проект** (efficient-enchantment)
3. **Убедитесь, что оба сервиса в одном проекте:**
   - `Postgres` - база данных
   - `numeric-duel` - ваш сервер

4. **Проверьте, что сервисы связаны:**
   - В Railway, когда сервисы находятся в одном проекте, они автоматически имеют доступ друг к другу
   - Но переменные окружения могут не передаваться автоматически

### Способ 2: Ручная настройка переменных окружения

Если автоматическое связывание не работает, нужно вручную добавить переменные окружения:

#### Шаг 1: Получите данные для подключения к PostgreSQL

1. Зайдите в Railway Dashboard
2. Выберите сервис **Postgres**
3. Перейдите на вкладку **"Variables"** (или "Settings" → "Variables")
4. Найдите следующие переменные:
   - `DATABASE_URL` (если есть)
   - `PGHOST`
   - `PGPORT`
   - `PGDATABASE`
   - `PGUSER`
   - `PGPASSWORD`

**Или используйте вкладку "Connect":**
- Там будет строка подключения, которую можно скопировать

#### Шаг 2: Добавьте переменные окружения в сервис numeric-duel

1. Зайдите в сервис **numeric-duel**
2. Перейдите на вкладку **"Variables"** (или "Settings" → "Variables")
3. Нажмите **"+ New Variable"**
4. Добавьте **DATABASE_URL** (если его нет):
   - **Key:** `DATABASE_URL`
   - **Value:** Скопируйте значение из сервиса Postgres
   - Формат обычно: `postgresql://user:password@host:port/database`

**Или добавьте отдельные переменные:**
- `PGHOST` = значение из Postgres сервиса
- `PGPORT` = значение из Postgres сервиса (обычно 5432)
- `PGDATABASE` = значение из Postgres сервиса
- `PGUSER` = значение из Postgres сервиса
- `PGPASSWORD` = значение из Postgres сервиса

#### Шаг 3: Перезапустите сервис

После добавления переменных окружения:
1. Сервис автоматически перезапустится, или
2. Вы можете вручную перезапустить через кнопку "Restart" в Railway

---

## 🔍 Проверка настроек

После добавления переменных окружения, проверьте логи сервиса `numeric-duel`:

### Успешное подключение:
```
📊 Используем DATABASE_URL для подключения к PostgreSQL
✅ Database initialized successfully
✅ Loaded X players from PostgreSQL
📊 PostgreSQL подключен, лидерборд загружен
```

### Если все еще есть ошибки:
```
❌ ОШИБКА: Не найдены переменные окружения для PostgreSQL!
❌ Проверьте настройки Railway:
   1. Убедитесь, что сервис Postgres активен
   2. Убедитесь, что сервис numeric-duel связан с Postgres
   3. Проверьте переменные окружения в настройках сервиса numeric-duel
```

---

## 📋 Пошаговая инструкция с скриншотами

### Вариант A: Использование DATABASE_URL

1. **В сервисе Postgres:**
   - Зайдите в "Variables"
   - Найдите `DATABASE_URL`
   - Скопируйте его значение

2. **В сервисе numeric-duel:**
   - Зайдите в "Variables"
   - Нажмите "+ New Variable"
   - **Key:** `DATABASE_URL`
   - **Value:** Вставьте скопированное значение
   - Нажмите "Add"

### Вариант B: Использование отдельных переменных

1. **В сервисе Postgres:**
   - Зайдите в "Variables"
   - Скопируйте значения:
     - `PGHOST`
     - `PGPORT`
     - `PGDATABASE`
     - `PGUSER`
     - `PGPASSWORD`

2. **В сервисе numeric-duel:**
   - Зайдите в "Variables"
   - Добавьте каждую переменную отдельно:
     - `PGHOST` = [значение]
     - `PGPORT` = [значение]
     - `PGDATABASE` = [значение]
     - `PGUSER` = [значение]
     - `PGPASSWORD` = [значение]

---

## ⚡ Быстрое решение

Если вы хотите быстро проверить, работает ли подключение:

1. **Зайдите в сервис Postgres в Railway**
2. **Найдите `DATABASE_URL` в Variables**
3. **Скопируйте его значение**
4. **В сервисе numeric-duel:**
   - Добавьте переменную `DATABASE_URL` с этим значением
5. **Сервис автоматически перезапустится**

---

## 🔗 Полезные ссылки

- [Railway Documentation: Variables](https://docs.railway.app/develop/variables)
- [Railway Documentation: PostgreSQL](https://docs.railway.app/databases/postgresql)
- [Railway Documentation: Service Networking](https://docs.railway.app/develop/service-networking)

---

## ✅ После настройки

После добавления переменных окружения:
1. Сервис автоматически перезапустится
2. Проверьте логи - должны увидеть сообщения об успешном подключении
3. Протестируйте игру - данные должны сохраняться в PostgreSQL
